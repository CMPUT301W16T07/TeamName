<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/black_chalkboard">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/SessionField"
        android:id="@+id/sessionButton" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:textAlignment="center"
        android:textSize="24dp"
        android:textStyle="bold"
        android:textColor="@color/white"
        android:text="@string/BidsOnMySessionsField"
        android:id="@+id/bidsOnMySessionsText" />

    <ListView
        android:layout_width="fill_parent"
        android:id="@+id/bidsListView"
        android:layout_height="450dp"
        android:layout_below="@+id/bidsOnMySessionsText"
        android:divider="@null"
        android:dividerHeight="0dp"/>
</LinearLayout>
